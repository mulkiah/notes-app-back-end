# install nodemon
npm install nodemon --save-dev

# run
npm run start

# install ESlint
/*Tools yang dapat membantu atau membimbing Anda untuk selalu 
menuliskan kode JavaScript dengan gaya yang konsisten.
*/

npm install eslint --save-dev

# init ESlint
npx eslint --init

#install nanoid
npm install nanoid@3.x.x

# to push in gitlab
git remote add origin https://github.com/mulkiah/notes-app-back-end.git
git push origin master